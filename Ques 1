#include <stdio.h>

int mult;

int calculate(int a, int b) {
    return mult = a * b;
}

int main() {
    int result = calculate(3, 5);
    int result1 = calculate(7,2);
    printf("%d\n", result);
    printf("%d\n", result1);
    return 0;
}